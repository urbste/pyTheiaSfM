build/*
*.user
*.user.*
wheelhouse
desktop_build/
*.ply
nose/
nose-*
auditwheel/
auditwheel-*
pyelf*
elftools*
debug_out*
build*
# Exception: MkDocs page name matches `build*` but must be versioned (strict link check in CI).
!docs/content/building.md
cmake_build*
*.csv

*.ply
*.json

.vscode

*.so

# Source for the following rules: https://raw.githubusercontent.com/github/gitignore/master/Python.gitignore
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

cmake_build/*
pytheia.egg-info
pytheia_build/*
dist/*

# Native extension + optional local CMake copies (never commit binaries)
src/pytheia/pytheia.cpython-*.so
src/pytheia/pytheia.*.so
src/pytheia/libflann_cpp*.so

# Generated stub directories (alternate tool output)
typings/

# MkDocs output (built locally or by CI before artifact upload)
docs/site/